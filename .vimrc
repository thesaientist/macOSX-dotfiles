"""""""""""""""""""""""""""""""""""""""""""""""""
syntax enable
set background=dark
colorscheme solarized

" vim editor configuration file

" display line numbers
set number

" perform incremental search when using search function
set incsearch

" ignore case in search if all in lower case, but if any upper case --> then do case sensitive search
set ignorecase
set smartcase

" show at least 5 lines of code below/above the line that is being edited
set scrolloff=5

" setting to show list of all matching files or subdirectories that will match the filename that is starting to be typed
set wildmode=longest,list

